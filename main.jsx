import React from 'react';
import ReactDOM from 'react-dom/client';
import JobstreamWidget from './JobstreamWidget';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <JobstreamWidget />
  </React.StrictMode>
);
